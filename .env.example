# 环境变量模板 —— 复制成 .env.local 后按需修改（.env.local 不会被提交）
#
# VITE_USE_MOCK
#   true（默认）→ 使用 src/mocks/ 下的本地演示数据，无需后端即可跑通全站
#   false       → 请求后端真实 API（需先启动 backend/）
#
# 注意：只有显式写成 false 才会走后端；未配置、写错或留空都会回落到演示数据，
#       以保证页面永远不会因为「后端不在」而整站空白。
VITE_USE_MOCK=true

# 后端 API 根地址（留空表示同源，由 vite.config.ts 的 /api 代理转发到 localhost:3000）
# VITE_API_BASE=http://localhost:3000
