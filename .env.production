# 生产环境（构建 / 部署）
#
# 当前没有随站点一起部署的后端服务，所以这里保持 true：
# 构建产物使用 src/mocks/ 下的本地演示数据，任何人打开链接都能看到完整内容，
# 不会出现「登录不上 + 页面全空」的整站空白。
#
# 如果你把 backend/ 用 docker-compose 起起来（含 MySQL），并把 /api 反向代理到它，
# 再把这个值改成 false，就会切换到真实 API：
#   VITE_USE_MOCK=false
VITE_USE_MOCK=true
